require 'spec_helper'

describe GemSamplePrudy do
  it 'has a version number' do
    expect(GemSamplePrudy::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
